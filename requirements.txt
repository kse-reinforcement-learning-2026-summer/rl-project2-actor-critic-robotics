# Your project has ONE real dependency: the frozen environment + grader, pinned to a release
# tag. Everything heavy (panda-gym, pybullet, gymnasium, numpy) is pulled in transitively.
# Stable-Baselines3 is NOT needed to run the tests — your model.pt is a standalone TorchScript
# file. The training stack is installed separately inside the notebook (the [train] extra).
panda-push-pixels @ git+https://github.com/kse-reinforcement-learning-2026-summer/panda-push-pixels.git@v11.0.0
pytest==9.0.3
